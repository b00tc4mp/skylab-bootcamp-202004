describe("authenticateUser",function(){
    false &&it("should return an error if the password and the mail doen't match",function(){
        const name="name1",
        surname="surname1",
        email="e@mail.com",
        password="password1";
        users.push(name,surname,email,password);
        
        user=
        
    
        expect(user).to.exist
    });
});