/*
require('dotenv').config()

const { env: { TEST_MONGODB_URL: MONGODB_URL } } = process

const addDelivery = require('./add-delivery')
const { random } = Math
const { expect } = require('chai')
const { mongoose, models: { Product } } = require('facturator-data')

describe("addDelivery",()=>{
    let clientName, clientEstablishment, clientContactNumber, clientEmail ,clientDirection,clientPaymentMethod,clientPaymentInfo
    let productName, productDescription, productPrice, productTax, productAlergens
})
*/